#!/bin/env python3
import cv2
from cv2_course.exercise_3 import cascade_paths

# face_cascade = cv2.CascadeClassifier()

# TODO: Read an image from a file and convert it to grayscale.

# faces = face_cascade.detectMultiScale()

# for i, (x, y, width, height) in enumerate(faces):
    # TODO: Convert the rectangle returned by detectMultiScale() to a square.

    # TODO: Crop the color image to the square and write it to a file.

